package com.admtechhub.maestrohr.attendance;

/** How a late/absence deduction amount is interpreted: a flat kobo sum or a percentage of salary. */
public enum DeductionType {
    FLAT,
    PERCENTAGE
}
